/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "source.unsplash.com",
      "plus.unsplash.com",
    ],
  },
};

module.exports = nextConfig
